name: Build Console Channel Strip X v0.4.1 Windows x64

on:
  workflow_dispatch:

concurrency:
  group: console-channelstrip-x-v041-windows
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: Windows build disabled for automatic runs
        shell: pwsh
        run: |
          Write-Host "Windows build is manual-only for now."
